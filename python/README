========================================================
== LINUX INSTRUCTIONS (tested on Ubuntu 14.04 64-bit) ==
========================================================

To run:
Run `./Game -s [board size]`, where [board size] is in the range [5, 10].
